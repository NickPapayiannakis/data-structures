OVERVIEW: RETREIVE
DESCRIPTION: Retreives the value at a key

1. If there is no bucket, we have nothing to return, hence null!
2. if there is, we look inside the bucket
3. when we are looking inside the bucket, if there is a
matching key in one of the tupals, return that key's value.


INPUT: a key
OUTPUT: value

TEST: should retrieve the value from a specified key

PSEUDO-CODE:
// set up var index
// set up var bucket
// if no bucket
  // return null
// otherwise
  // loop inside the bucket
    //if matching key found
      // return value;
    // otherwise return null

